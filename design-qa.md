# R2 首屏验收记录

日期：2026-09-23。需求与决策唯一来源：[landing-page-ssot.md](docs/landing-page-ssot.md)。本文件仅保存技术验证，不另立产品需求。

## 验收范围

本地首页 `http://localhost:3100/` 的新导航、V4/V5/V6 轮播、四科提示、手机/平板排版，以及到现有报名区的跳转。不是整站发布验收；未提交表单、未创建账户、未改线上站点。

## 视觉基准与对照

- 基准：`docs/assets/landing-page/2026-09-22/landing-01-warm.png`，1024×1536 的第一版长图；比较其页首到四科条的位置。第二、第三版不作为基准。
- 实现：`docs/assets/landing-page/2026-09-23-r2/desktop-1440.jpg`。按首屏区域和相对位置比较，未以整张长图与首屏截图计算像素误差；两者构图比例不同，不宣称逐像素复现。
- 保留：暖白背景、自然校园摄影、深蓝宋体大字、蓝色主按钮、左文字右人物、产品参考画面。
- 有意调整：固定品牌 Logo；当前确认的申请内测 CTA、30 位与两周信息；四科；三个故事切换；手机/平板上下构图。去掉原图未经确认的视频课、示例成绩和装饰手写文字。
- 生成照片实际为 1672×941。三个高清网页截图以 2560×1440 视口采集，浏览器输出 JPEG 为 2552×1436；文件名中的 2560 是视口宽度。截图不是原生 2560 摄影素材，也不是无界面元素的广告印刷稿。

## 检查与修正

| 检查项 | 结果与证据 |
| --- | --- |
| 桌面 1440×900 | 标题、照片、导航、CTA 清晰；截图 `desktop-1440.jpg` |
| 高清视口 2560×1440 | 三主题截图 `hero-v4-2560.jpg`、`hero-v5-2560.jpg`、`hero-v6-2560.jpg`；视觉检查通过 |
| 手机 390×844 | 新导航正常，独立上下构图；`mobile-390.jpg` |
| 窄屏 320×740 | 最长标题不溢出、CTA 可见；`mobile-320-parent.jpg` |
| 平板 768×1024 | 改为上下布局，解决标题压住人脸；给旧反馈悬浮按钮留出空间；`tablet-768.jpg` |
| 水平溢出 | 浏览器读到 320/390/768 视口的内容宽度分别为 312/382/760，不超过视口；差值来自滚动条 |
| 手机导航 | 展开成功；Escape 收起并回到菜单按钮；解决旧外层 overflow 导致负边距导航被裁切的问题 |
| 手动轮播 | 三个主题选择器、前后按钮、键盘 ArrowRight 切换成功；当前项 aria-pressed 标记正确 |
| 自动轮播 | 默认关闭；手动启动后离开轮播区域，观察到从 1 到 3；可暂停，手动选择后回到播放按钮 |
| 报名 CTA | 到达 `/#beta`，邮箱字段可见；不提交、不声称筛选表已接通 |
| 可访问性实现 | 单个 h1，图片 alt，按钮名称，跳过导航，焦点边框；轮播箭头触控区域 44px；减少动态偏好下停止动画与自动播放 |
| 运行错误 | 交互检查时控制台 error/warn 为空。构建过程中曾出现旧预览进程读取被替换构建的临时错误，停旧进程后启动最终构建恢复 |

触摸滑动已实现，但没有物理触屏实测；减少动态模式已做代码检查，未改变用户系统设置测试。浏览器快照为实际渲染证据，不用静态代码代替视觉验收。

## 自动检查

- `npm run lint:src`：通过。
- `./node_modules/.bin/tsc --noEmit`：通过；后续最终构建也运行 TypeScript 检查。
- `npm run build`：通过，88 个静态页面生成完成。
- `git diff --check`：通过。
- 现有 Browserslist 数据过期提示不阻塞构建；未为本次 UI 改动升级依赖。
- 本地 `pnpm dev` 因依赖目录检查要求交互而未启动；使用现有同一脚本的 `npm run dev`，最终预览切换为 `npm run start -- --port 3100`，没有重装或删除依赖。

## 后续范围／非阻塞项

- 产品截图是明确标注的设计参考，真实四 tab 画面待 R3。
- 下方内容和页脚仍是旧版本，套餐页仍有旧价格；R4 更新后才能进行整站发布验收。
- 当前保留原邮箱登记入口；Google Forms、学习情况筛选、WhatsApp 流程尚未接通，不能对外宣称已完成。
- 需要 R5 进行真机、性能及完整无障碍验收；本次响应式检查不替代全部发布测试。
- 未更名目录、拆分旧 Web App、改 App、改数据库、接支付或部署。

最终结果：passed（限上述 R2 首屏范围，不代表整站已可发布）。

## R2 用户反馈修订验收（2026-09-23）

- 字体方向 A：Noto Serif SC 标题字重由 900 调至 700，字距放松；后续段落标题使用 600。顶部采用 LXGW WenKai TC，浏览器计算样式确认已加载；黄色弧线用装饰 SVG，文字仍可选择与朗读。
- 导航/页脚品牌为 Learnbank 配 .ai 圆角标签；正文、公开官网元数据与静态法务页的品牌统一 Learnbank.ai。
- 桌面 1440×900、手机 320×740 实测无水平溢出；最长家长标题完整显示，手机品牌与菜单不相撞。
- 桌面与手机的「价格方案 待确认」链接均能进入 `/pricing`；小标签定位在文字右上角。价格页手机单列；旧金额、旧月订/年订和折扣文案已移除，正式版规划与内测权益明确区分。
- 页脚实际渲染内容已检查：产品、学习资源、联系以及所有链接名称均为中文；联系地址与 mailto 均为 help@learnbank.ai。
- 首页申请表占位文字改为「微课笔记」，保留 iOS/Android、WhatsApp 等平台专名。
- 源码 lint、TypeScript 与生产构建通过；浏览器未记录 error/warn。没有提交表单或发送邮件。
- 本轮证据位于 `docs/assets/landing-page/2026-09-23-r2-feedback/`。上方旧截图与旧价格描述为 R2 初版历史；当前以本节和 SSOT v0.5 为准。
- 事务邮件后台的发件域名未改；邮箱开通/认证不属于本轮文案修改。第 2–8 部分仍未按整页新方案完成，未进入 R3。

修订最终结果：passed。

## R2 第二次反馈修订

三页主标题取消差异字号，副标题统一 14px，切换区改正常布局独立行。1200px 三页标题均 54px，申请按钮纵向位置均为 476.6875px；320px 第一/第三页标题均 29px、按钮均为 334.375px。390px 科目页只有一个 header，内容宽 382px，无横向溢出。科目页复用价格页的排版类与统一导航、页脚；桌面和手机截图存为 `subjects-desktop.jpg` / `subjects-mobile.jpg`。没有改首页下方 01–06 内容，也未添加暗色模式。

本次源码 lint、生产构建（包含 TypeScript）与 `git diff --check` 均通过。最终结果：passed（本次 R2 修改范围）。


## R3 第一版验证（2026-09-23）

- 新增 `LearningJourney.tsx` 与 CSS module；首页替换旧 01–03，保留 R2 首屏与旧 04–06。
- 微课示意：选择向上显示纠错提示，选择向下显示正确反馈，可重新选择；不保存记录。
- 四个功能选择器可切换标题、说明和对应设计参考；明确标注历史素材、旧科目/套餐及示例数值。
- 1440×1000 桌面检查；390×844 / 320×740 手机实际内容宽度为 382 / 312，无水平溢出，答题按钮与四功能按钮可操作。
- 新 CTA 到达 `/#beta`，没有提交表单。控制台 error/warn 为空。
- 截图：`docs/assets/landing-page/2026-09-23-r3/desktop-analyse.jpg`、`mobile-390-features.jpg`、`mobile-320-lesson.jpg`。浏览器输出为 JPEG。
- `npm run lint:src`、`npm run build`（含 TypeScript，88 个静态页面）通过；Browserslist 旧数据提示仍存在，没有升级依赖。
- 真实 App 截图、跨 tab 实际数据/推荐/分析链路未验证；不把设计参考当实际功能证据。正式表单、真机触摸、完整无障碍与性能验收仍待后续。

结果：passed（限 R3 交互展示初版）；R3 最终验收待真实画面和用户反馈。当前进度以 SSOT v0.6 为准。

## R3 手机展示反馈修订（2026-09-24）

- 01 改为手机外框与 App 结构的前端微课演示；热身/讲解/公式/检查点可切换，逐段展开、前一步/重新开始、先选后检查、错误重选、正确后再体验正常。
- 02 桌面左侧四功能选择、说明、右侧手机；移动端横排选择器。不加载旧参考图、不提供放大，移除用户指定的两段素材文案。新画面为简化前端演示，含“示例数据”，待实机视觉核对。
- 1440 桌面截图与 320/390 手机截图存于 `docs/assets/landing-page/2026-09-24-r3/`。修复 grid 自动最小宽度导致的手机外框裁切：320px 下容器 x=24、width=264；整页宽 312。390px 整页宽 382。控制台无 error。
- 源码 lint、TypeScript、diff 格式检查通过。生产构建首轮 Google 字体模块解析失败；未变更依赖，重试 `npm run build` 成功。
- 03 三个新网页区块渲染方案仅存档待选，不替换当前页面。生成稿中附加导航、按钮和未经核验的能力措辞不作为实施决定。

结果：上述前端交互与响应式检查通过；真实 App 对照、真实学习数据闭环及发布验收仍待后续。

## R3 九模式／三功能／三卡片验收（2026-09-24，当前）

### 视觉基准和实现证据

- 01 基准：App `design/refs/L04B.png`（854×1844），以及当前 `lessonPlayerTokens` / `lesson-player.tsx` / `learn-screen.tsx`。对照状态为检查点第一项选中、尚未检查；同一工具结果中打开源图和浏览器页面比较。手机 CSS 尺寸 393×820，截图为外围页面 1432×1094（1440×1100 视口）；按手机内容区比例比较，不作整页像素差计算。
- 01 证据：`docs/assets/landing-page/2026-09-24-r3-final/desktop-lesson.jpg`、`mobile-320-lesson.jpg`（312×722，320×740 视口）。实际 App 图标与数学图直接复用，无生成替代图。
- 02 基准：`public/images/landing-r3/practice-reference.png`、`ask-reference.png`、`analyse-reference.png`。恢复原图的功能区域，CSS 裁切隐藏套餐/额度/范围外科目；不修改原图片。`static-analyse.jpg` 已检查，保留指标、薄弱点和下一步区域，省略 Basic/Pro 套餐。
- 03 基准：`docs/assets/landing-page/2026-09-24-r3/accumulation-{a-learning-history,b-next-step,c-knowledge-record}.png`。A/B/C 源图与实现均在工具中打开比较，证据为 final 目录的 `accumulation-a.jpg` / `accumulation-b.jpg` / `accumulation-c.jpg`。按用户批准，采用内容与构图做网页卡片，不采用生成稿附带导航/注册入口、不逐像素临摹整张图片。
- 手机 C 卡片下半部：`mobile-390-record.jpg`（382×827，390×844 视口）；知识记录、状态和总结可读，无横向裁切。临时验证标签关闭，用户原预览保留。

### 五项视觉检查

- 字体：外部暖色宋体标题延续 R2；手机内使用独立无衬线样式，25px 标题、13.5px 正文，按 App token 对齐；模式入口无英文、两列紧凑排布。App 与浏览器字体渲染不同，不宣称逐像素一致。
- 间距：手机图文卡、四选项、提示、底部按钮与课程栏按 App 结构布局；内容可内部滚动。九模式进度替代原六节，顶部重置替代关闭，移除示例 XP，是官网演示的明确差异。
- 颜色：手机 #fbfcff 背景、#2f73ff 选中/按钮；答题三段反馈保留紫/绿/琥珀层次；官网卡片保持暖白/深蓝/蓝色动作与黄色指示。
- 素材：L04 数学图与图标沿用原件，02 图片没有再用简化网页面板重绘；03 使用网页文字和已有图标库呈现用户批准的概念。
- 内容：Learn 只在 01 展示；02 仅三项；03 数据为学习过程示例，未虚构用户成绩、账户、真实推荐结果。

### 问题与修正

- [已修正] 配对点击处理器一处 JSX 闭合错误，开发预览恢复；最终 lint/build 通过。
- [已修正] 静态功能切换时旧图片可能短暂留存，按图片 key 重新挂载；验证分析资源对应 analyse-reference.png。
- [已修正] 检查点选中状态缺少参考图的勾选标记；补回并复查。
- 没有剩余阻塞本轮前端交付的布局/交互问题。真实最新 App 的每一屏精确像素对照仍属后续素材核验，不以此次结构对照代替。

### 交互与检查

- 六种阅读模式逐段展开分别实测；检查点错选→检查→重试→正确、判断题正确反馈、三组函数配对成功均实测。
- 03 名称按钮、左右按钮、键盘方向键、末张回首张通过；触摸水平滑动已实现，未用真实触屏验证，不将鼠标测试称为手势验收。
- 320px：页面内容 312px，手机 x=24、width=264；390px：内容 382px；无横向溢出，手机底部操作保留。
- 独立验证标签控制台 error 为空；未提交报名、未调用后端。
- 最终 `npm run lint:src`、`npm run build`（含 TypeScript）通过，`git diff --check` 通过。保留本地预览，未部署。

final result: passed

范围：用户确认的本轮 R3 官网演示实现；不代表真实推荐/分析数据链路、触屏真机或整站发布验收通过。R3 视觉定稿等待用户反馈，04–06 未进入 R4。

## 2026-09-24：R3 视觉反馈精修复验（当前最新）

**视觉基准与浏览器证据：**
- 首屏手机：基准 `docs/assets/landing-page/2026-09-22/landing-01-warm.png`（1775×887）；实现 `docs/assets/landing-page/2026-09-24-r3-refinement/qa/hero-desktop.png`（1432×1000，浏览器 CSS 1440×1000，密度 1）。同张对照图 `qa/hero-comparison.png`，左右各归一到 720×540，只比较首屏的手机质感、位置与信息层次，不比较原图一整页与现实现的不同页面长度。
- 03 A/B/C：基准 `docs/assets/landing-page/2026-09-24-r3/accumulation-*.png`，其中 B 的用户附图对应 `exec-5f136486-c546-420c-83d1-070afb14b77a.png`；实现 `qa/accumulation-{a,b,c}-desktop.png`。同张左右对照图 `qa/accumulation-{a,b,c}-comparison.png`，均归一到 720×540。基准是完整渲染图，实现截图含网页容器/控制区；据此比较板块内部层级、布局与视觉密度，不作像素重合判断。
- 01、02 实际浏览器截图为 `qa/learn-desktop.png`、`qa/loop-desktop.png`；手机截图 `qa/loop-mobile-390.png`、`qa/accumulation-mobile-390.png`、`qa/accumulation-mobile-320.png`。源布局以本轮用户反馈附图和旧功能原图为准。截图由 in-app Browser 获取，未用 HTTP 检查代替视觉验收。

**首次对照问题与修正：**
- [P1 已修] 01 上轮手机 393×820、整块过高：桌面手机按 0.76 比例呈现，板块改为约 726px，与当前三张首屏轮播的约 726px 相当；宽度保留响应式。默认讲解状态加入数学图和更多阅读内容，让手机初始展示有内容层次。复查 `learn-desktop.png`；手机内容可内部滚动。
- [P1 已修] 02 原 780px 最小高度和长引言造成大空白：删除该段及最小高度，内容贴近标题；复查 `loop-desktop.png`。1440px 桌面板块 766px，约为首屏的 1.055 倍。
- [P1 已修] 02 三张图原高度不一：练习、答疑、分析统一裁切框实测均为 290×475px，切换时整个板块均 766px，无布局跳动。Analyse 图底部旧 Pro 权益区已裁出可见区域。旧图内其余文字和数字仍为设计参考，并非当前真实学习结果。
- [P1 已修] 03 上轮统一卡片布局过素：A 还原三阶段卡片、蓝色强调词与大图标；B 还原三条记录与汇聚连线、分析节点和右侧推荐面板；C 还原时间线、知识档案、状态标记与底部浅蓝总结条。检查 `qa/accumulation-{a,b,c}-comparison.png`。原稿自带的导航没有在 03 内重复，文字保持网页可选取。
- [P2 已修] 首屏右下原手机是小截图裁切：换成透明背景的黑色完整机身素材，含中文数学章节列表和深蓝继续学习卡；调整位置，使首屏轮播控制保持可见。见 `qa/hero-comparison.png`。

**五项视觉检查：** 标题仍用既定暖色宋体和深蓝；01–03 的间距、对齐和板块高度均在浏览器中复查；蓝/黄强调色与 R2 统一；首屏手机资产清晰、透明边缘未出现白底方框；页面文案及功能分类按已确认的中文版本。03 为网页组件近似实现，图标形态与旧生成图存在小差别，但主体构图和信息层级保持一致。

**响应式和交互：** 1440×1000、390×844、320×740 浏览器视口检查。320px 时页面可见内容宽 312px，03 三张切换后文档宽均 312px，无横向溢出。02 练习/答疑/分析三次切换图片与文字正确，固定尺寸不跳。03 三张选择、前后按钮可用；键盘及触摸逻辑沿用上一轮实现，真实手指滑动仍未进行物理设备验收。该验证标签控制台 error 为空。`npm run lint:src`、`npm run build`（含 TypeScript）、`git diff --check` 通过；本地页面 HTTP 200。

**新概念图边界：** 5「初一至初三、四科范围」与 6「家长真实困扰」各三张概念图在 `docs/assets/landing-page/2026-09-24-r3-refinement/concepts/`，尚待 Victor 选择，未进入网站源码。A 四科图原书脊英文已单点编辑为中文。概念图含示例场景，不等于真实 App 功能或学生反馈。

**后续：** 最新真实 App 截图、真实数据链路、报名筛选表与公开发布不属于此次视觉复验。R3 的最终审美取舍仍待 Victor 复核；本轮已无可见 P0/P1/P2 布局问题。

final result: passed

## 2026-09-24：R4 已选 5/6 两块接入

**视觉基准：** 5 采用 `docs/assets/landing-page/2026-09-24-r3-refinement/concepts/subjects-cards-zh.png`，6 采用同目录 `parents-beside.png`。桌面端直接使用两张 1586×992 完整渲染图，按容器宽度等比缩放；浏览器 1440×1000 时两块各约 896px 高，原图标题、四科学习卡、家长场景和蓝色按钮均完整保留。图像素材复制到 `public/images/landing-r4/`，源图保留。

**响应式检查：** 浏览器 390×844 与 320×740 下，海报的正文和 CTA 改为真实网页文字与链接；四科照片从同一张已选图裁切，家长照片从另一张已选图裁切。首次复查发现数学照片左侧露白、家长照片边缘带出海报文字，已调整裁切并复查。320px 下四科标题曾多出孤立的末字，断点字号已修正为两行。两个宽度下文档均无横向溢出。

**功能与范围：** 两张桌面图的蓝色按钮热区、手机端两个 CTA 均指向现有 `#beta`；桌面已实际点击验证 URL 变化。旧学生/家长占位卡片被新版 6 取代，01–03 未修改。家长照片是生成的情境图，不作为真实用户见证。报名区仍为意向登记，未创建 Google Form 或提交数据。

**验证：** `npm run lint:src`、`npm run build` 通过；后续仍需核对最终真实 App 素材和报名筛选链路。此结果只表示 5/6 两块本地视觉与跳转接入完成，不代表整个 R4 或公开发布完成。

## 2026-09-24：R4 页面结构与跨尺寸复验（当前最新）

**目标与结果：** 四科 A 图已从首页移至 `/subjects`；首页保留家长 A 场景，并接上原生网页 FAQ、内测安排、申请入口、联系及法务页脚。`/pricing` 已改为免费版/Pro 卡片及四项权益比较。未生成新渲染图。

**宽屏字号：** 浏览器在 3840×2160 下实测首页首屏标题 140px、01 微课标题 96px、FAQ 标题 88px、价格页主标题 100px；首屏高度约 1450px，没有随 4K 宽度被拉到两千多像素。01 手机演示放大到约 452×943px；页面文档宽 3832px，小于 3840px 视口。1280×720 下 FAQ 两栏和价格页两档卡片可读。

**手机排版：** 390×844 检查 FAQ、申请表、价格页；320×740 检查首页、`/subjects`、`/pricing`，文档内容宽 312px，无横向溢出。`/subjects` 的标题曾被全局 h1 规则放大，末字单独换行，现以手机断点 26px 修正为两行。页脚最初被旧全局样式覆盖成白底，浅色文字对比不足，现固定为深蓝底；申请表标签在深色卡片内为浅色字。

**链接与交互：** `/subjects` 图内按钮到 `/#beta` 已点击验证；FAQ 项可展开，FAQ 到 `/pricing` 与价格页到 `/#beta` 已点击验证。构建时开发标签曾出现一次 HMR 模块失效后的回退导航；新标签复验 FAQ 到价格页正常，控制台无 error。未提交任何真实报名资料。

**校验与边界：** `npm run lint:src`、`npm run build`（含 TypeScript）通过，`git diff --check` 通过。当前表单仍是原有邮箱/设备/意向登记，不是最终 Google Forms 筛选表；未定套餐用量、奖品评选细则和真实 App 能力仍需发布前确认。

final result: passed（本轮前端范围）
