# Gemini UI Analysis

- Model: `gemini-2.5-pro`
- Image: `/Users/victorsim/Desktop/Screenshot 2026-03-11 at 3.25.58 PM.png`
- Target: `题目审核页 KPI 区`
- Goal: `参考批量导入页 KPI 卡片样式，优化题目审核页的审核概览卡片层级、尺寸与一致性；并考虑加入时间筛选入口，只输出结构实现建议`

## Raw Response

{
  "screen_summary": "参考批量导入页面的仪表盘样式，优化题目审核页的概览卡片区域。核心目标是统一视觉风格、提升信息层级清晰度，并增加时间维度筛选功能，以改善数据可读性和用户操作效率。",
  "layout": [
    "建议采用 CSS Grid 布局来组织 KPI 指标卡片，例如 `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6`，以实现响应式和统一的间距。",
    "在卡片网格的上方或页面标题旁，为新增的时间筛选器预留明确的布局空间。"
  ],
  "components": [
    "创建一个可复用的 `KPICard` React 组件，其 props 应包括 `title` (标题), `metric` (主指标), `description` (描述文本), `icon` (图标), 和 `status` (状态，如 'success', 'warning', 'info')，用于控制卡片右上角图标的颜色。",
    "引入一个日期范围选择器或预设时间范围（如“今日”、“近7天”）的下拉菜单组件，用于筛选 KPI 数据。"
  ],
  "visual_issues": [
    "推测当前题目审核页的概览卡片在尺寸、间距和视觉风格上不一致，导致页面视觉混乱。",
    "信息层级可能不清晰，主次信息（如核心指标和辅助说明）没有通过字号、字重或颜色进行有效区分。",
    "状态表达不统一，缺少像参考图中那样通过颜色和图标明确指示“成功”、“警告”等状态的视觉语言。"
  ],
  "ux_issues": [
    "缺少按时间维度查看数据的能力，用户无法快速了解不同时间段内的审核效率和积压情况。",
    "如果卡片设计不一致，会增加用户的认知负荷，降低信息获取效率。",
    "推测当前卡片可能缺少对空状态或加载状态的优雅处理，例如在数据加载时显示骨架屏（Skeleton）。"
  ],
  "design_tokens": {
    "colors": [
      "背景色: 深灰色（如 Tailwind `slate-900`）",
      "卡片背景色: 次级深灰色（如 `slate-800`）",
      "主文本色: 亮灰色（如 `slate-100`）",
      "辅助文本色: 中灰色（如 `slate-400`）",
      "状态色: 绿色(成功)、黄色(待处理)、紫色(警告)等，用于图标背景和指示器。"
    ],
    "spacing": [
      "卡片内边距: 统一使用 `p-6`",
      "卡片间距: 统一使用 `gap-6`"
    ],
    "radius": [
      "统一所有卡片、按钮的圆角，如 `rounded-xl`。"
    ],
    "typography": [
      "主指标: 大字号、粗体，如 `text-5xl font-bold`",
      "标题: 中等字号，如 `text-sm font-medium`",
      "描述文本: 小字号，如 `text-xs`"
    ]
  },
  "implementation_plan": [
    "1. 使用 React 和 Tailwind CSS 创建一个通用的 `<KPICard>` 组件，封装所有样式和逻辑。",
    "2. 在题目审核页面，使用 CSS Grid 替换现有的概览布局，并使用新的 `<KPICard>` 组件渲染数据。",
    "3. 集成一个日期选择组件（如 `react-datepicker`），并将其状态与数据请求逻辑关联。",
    "4. 修改数据获取函数，使其能够接收时间范围参数，并在日期选择变化时重新获取数据。",
    "5. 为卡片组件添加加载状态（如骨架屏）和空状态的展示逻辑。"
  ],
  "guardrails": [
    "确保 `<KPICard>` 组件具有足够的灵活性，能处理数值、百分比和纯文本等不同类型的指标内容。",
    "网格布局必须经过严格的响应式测试，确保在从小屏幕到大屏幕的设备上都能良好显示。",
    "所有交互元素和状态颜色需满足 WCAG 的可访问性对比度要求。",
    "避免在组件内部硬编码文本，应通过 props 传入，以支持国际化和复用。"
  ]
}
