import type { WebImportAdapter } from '../types'
import { examcooViewAdapter } from './examcoo.adapter'

export const WEB_IMPORT_ADAPTERS: WebImportAdapter[] = [examcooViewAdapter]

export { examcooViewAdapter }
