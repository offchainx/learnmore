# LearnMore v1.0 路由结构说明

## 📂 清理后的规范路由结构

```
src/app/
├── page.tsx                          → /                (Landing Page 营销首页)
├── layout.tsx                        → 全局布局
├── globals.css                       → 全局样式
│
├── (auth)/                           → 认证路由组（不影响URL）
│   ├── layout.tsx                    → 认证页面布局
│   ├── login/page.tsx                → /login
│   └── register/page.tsx             → /register
│
├── (dashboard)/                      → 仪表盘路由组（不影响URL）
│   ├── layout.tsx                    → 仪表盘布局（侧边栏+导航）
│   └── dashboard/                    → 所有需要登录的功能
│       ├── page.tsx                  → /dashboard        (主仪表盘)
│       ├── settings/page.tsx         → /dashboard/settings
│       ├── leaderboard/page.tsx      → /dashboard/leaderboard
│       ├── practice/                 → /dashboard/practice (开发中)
│       │   ├── debug/page.tsx
│       │   └── import/page.tsx
│       └── community/                → /dashboard/community
│           ├── page.tsx
│           ├── new/page.tsx
│           └── [postId]/page.tsx
│
├── course/                           → 课程相关页面
│   └── [subjectId]/
│       ├── page.tsx                  → /course/[subjectId]
│       └── [lessonId]/page.tsx       → /course/[subjectId]/[lessonId]
│
├── error-book/page.tsx               → /error-book
│
└── [其他营销页面]/                    → /about-us, /pricing, /blog 等
    ├── about-us/
    ├── pricing/
    ├── blog/
    └── ...
```

## ✅ 已删除的冗余文件

以下文件已被删除，因为它们永远不会被访问（死代码）：

- ❌ `src/app/(dashboard)/page.tsx`
  - **原因**: 优先级低于 `src/app/page.tsx`，永远不会被路由匹配
  
- ❌ `src/app/(dashboard)/settings/`
  - **原因**: 用户实际访问的是 `/dashboard/settings`，不是 `/settings`

## 🔑 关键设计原则

### 1. 路由组 (Route Groups)
- `(auth)` 和 `(dashboard)` 是路由组，**不会出现在URL中**
- 用途：共享布局，不影响实际路径

### 2. 路由优先级
当多个路由文件可能匹配同一路径时，Next.js 使用以下优先级：
```
app/page.tsx > app/(group)/page.tsx
```

### 3. 认证保护
`middleware.ts` 处理路由保护：
- 未登录访问 `/dashboard/*` → 重定向到 `/login`
- 已登录访问 `/login` 或 `/register` → 重定向到 `/dashboard`

## 📊 路由统计

- **营销页面**: 12个 (/, /about-us, /pricing, /blog, etc.)
- **认证页面**: 2个 (/login, /register)
- **仪表盘页面**: 8个 (/dashboard/*)
- **课程页面**: 2个 (/course/*)
- **其他功能页面**: 1个 (/error-book)

**总计**: 25个有效路由

## 🛡️ 安全性

- 所有 `/dashboard/*` 路由受 middleware 保护
- 使用 Supabase Auth + Cookie-based sessions
- Session 有效期：1小时（滑动窗口）

## 📝 维护建议

1. **新增功能页面**：统一放在 `(dashboard)/dashboard/` 下
2. **命名规范**：使用小写 + 连字符（如 `practice-center`）
3. **避免冗余**：每个功能只创建一个路由文件
4. **文档更新**：新增页面后及时更新 `docs/stories/项目结构&文件介绍.md`

---

**最后更新**: 2026-01-13  
**清理完成**: 删除了 2 个冗余文件，路由结构现已规范化
