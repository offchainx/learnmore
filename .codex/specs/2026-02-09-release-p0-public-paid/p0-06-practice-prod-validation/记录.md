1) 当前与练习题相关的数据表有哪些

按“直接练习链路”到“题库支撑”分层：

直接练习记录表
questions（题目主体）schema.prisma (line 335)
exam_records（一次练习/考试提交汇总）schema.prisma (line 424)
user_attempts（用户逐题作答明细）schema.prisma (line 400)
error_book（错题与掌握度）schema.prisma (line 450)
题库结构与内容生产表
subjects、chapters、chapter_prerequisites（学科/章节结构）schema.prisma (line 211)
source_files（题目来源文件）schema.prisma (line 706)
question_groups（套卷/组合题）schema.prisma (line 734)
题目标注与质控表
question_tags、question_tag_relations schema.prisma (line 769)
knowledge_points、question_kp_relations schema.prisma (line 806)
content_review_logs、question_reports schema.prisma (line 846)
练习后联动（间接相关）
leaderboard_entries、daily_tasks、user_badges、notifications（积分/任务/成就/通知）
2) 当前表设计里不合理点、缺失字段、可删减字段

先说高优先级问题（影响数据正确性）：

不同练习模式的数据落库不一致
SmartDrill 和 ChapterDrill 目前主要是前端本地判题，不落 user_attempts。
SmartDrillMode.tsx (line 32)
QuizSession.tsx (line 50)
DrillInterface.tsx (line 52)
error_book.masteryLevel 语义冲突
submitQuiz：答对置 0，答错递增 quiz.ts (line 135)
submitExam：错题也置 0 exam.ts (line 467)
同一个字段在不同链路含义不一致，后续分析会失真。
缺乏提交幂等键
submitQuiz 每次提交都会新建 exam_record + user_attempts，重复点提交会重复入库。
quiz.ts (line 107)
抽题未限制 questions.status
目前抽题没有过滤 PUBLISHED/VERIFIED，草稿题可能被抽到。
data-service.ts (line 388)
exam.ts (line 132)
中优先级（建模缺口）：

外部来源追溯字段不完整
建议在 questions 或新增映射表补：source_platform/source_paper_id/source_question_id/source_url/source_category_k/import_batch_id。
当前主要依赖 source_files/question_groups，但对“单题来源定位”不够直接。
exam_records 缺“题目快照/幂等信息”
建议增加：idempotency_key、question_set_snapshot(json)、started_at/submitted_at。
当前 startExam 只在 sessionStorage 暂存题集，服务端不持久化题目集合快照。
user_attempts 缺“作答过程”字段
建议增加：answer_changed_count、client_duration_ms、sequence_no、attempt_source（smart_drill/chapter_drill/mock_exam）。
低优先级（可优化/可删减）：

可能冗余或未收敛字段
QuestionType.MCQ 与 SINGLE_CHOICE/MULTIPLE_CHOICE 重叠，且判题逻辑未完整支持。
schema.prisma (line 391)
chapter.cachedMastery 这种 JSON 按用户聚合字段长期会膨胀，建议迁移到独立统计表。
schema.prisma (line 239)
3) 当前题目入库逻辑 + 用户交互采集逻辑

题目入库（现状）有两条主路：

快速录题（图片解析后手动保存）
前端上传图片 -> parseQuestionImage 识别 -> createQuestion 入库。
parser.ts (line 52)
question.ts (line 16)
特点：快，但来源字段弱、去重能力弱（这条路径没走 content_hash 去重）。
Content Pipeline 批量导入
importFromPDF：建 source_files -> OCR -> AI 结构化 -> bulkCreateQuestions。
import-service.ts (line 64)
bulkCreateQuestions 使用 content_hash 去重。
question-service.ts (line 174)
用户与练习题交互（现状）：

课程 Quiz（会落库）
用户答题 -> 点击提交 -> submitQuiz。
QuizView.tsx (line 57)
后端事务：
写 exam_records
批量写 user_attempts
upsert error_book
再触发 leaderboard / streak / daily task / badge
quiz.ts (line 107)
Mock Arena（会落库）
开考 startExam 先建 exam_record。
exam.ts (line 168)
交卷 submitExam 写 user_attempts，错题写 error_book。
exam.ts (line 247)
Smart Drill / Chapter Drill（目前大多不落库）
主要是前端本地状态判题与展示，没有统一提交到 user_attempts。
这正是你这次梳理里最关键的结构问题之一。
如果你同意，我下一步（新的 T-005）会先做一版“练习统一提交网关”设计：让 SmartDrill / ChapterDrill / Quiz / MockArena 都走同一套 attempt/event 入库标准，再接 Examcoo 导入脚本。


3. 练习主链路函数（后端）

统一提交入口：submitPracticeSession(...)，写入 exam_records + user_attempts，文件：session.ts。
普通测验提交：submitQuiz(...)，实际调用统一入口并触发任务/积分，文件：quiz.ts。
模拟考：startExam / submitExam / getExamResult，文件：exam.ts。
抽题与章节统计：getRandomQuestions / getSubjectChapters / getChapterWithStats / getWeaknessAnalysis，文件：data-service.ts。
Smart Drill 推荐：getSmartDrillQuestions，文件：recommendation.ts。
错题清扫：getErrorWiperSession / updateErrorWiperProgress（基于 attempts 聚合），文件：error-book.ts。
统计：getKnowledgeHiveData / getExamForecastData，文件：statistics.ts。
4. 页面入口与 API 入口

练习中心首页：/dashboard/practice，文件：practice/page.tsx。
科目数据 API：/api/practice/bootstrap、/api/practice/subject-data，文件：bootstrap/route.ts、subject-data/route.ts。
Smart Drill 页面：/dashboard/practice/smart-drill，文件：smart-drill/page.tsx。
Error Wiper 页面：/dashboard/practice/error-wiper，文件：error-wiper/page.tsx。
Mock Arena 页面：/dashboard/practice/mock-arena 与 /dashboard/practice/mock-arena/[examId]，文件：mock-arena/page.tsx、[examId]/MockArenaExam.tsx。
Chapter Drill 页面：/dashboard/practice/chapter-drill/[chapterId]，文件：chapter-drill/page.tsx。
Past Paper 页面：/dashboard/practice/past-paper/[paperId]，文件：past-paper/page.tsx。
内容管理入口：/admin/content/import、/admin/content/review、/admin/content/statistics、/admin/content/reports，对应文件在 src/app/(dashboard)/admin/content/...。
5. 从“选科目到五种练习方式”的实际链路

选科目：PracticeView 先调 /api/practice/bootstrap，再按科目调 /api/practice/subject-data。
Smart Drill：有入口，有后端抽题函数。
Error Wiper：有入口，有后端队列与进度写入。
Mock Arena：有入口，有 start/submit 全流程。
Chapter Map（章节练习）：有入口与抽题，但当前答题过程主要是前端本地流程。
Past Year Paper：有入口，题目按 is_past_paper + paper_id 拉取，提交走 submitQuiz。
6. 当前关键差距（很重要）

Smart Drill 当前 QuizSession 是前端本地判分，没有调用 submitPracticeSession 落库。
文件：QuizSession.tsx。

Chapter Drill 当前也是前端本地流程，且无题时有 mock fallback。
文件：DrillInterface.tsx、chapter-drill/page.tsx。

/admin/content/import 页面主体仍在用 MOCK_* 组件数据渲染，不是完全真数据面板。
文件：ImportClient.tsx。

/admin/content/statistics 现用静态常量组件，不走 getContentStats()。
文件：statistics/page.tsx、content-statistics/*。

reportQuestion/getQuestionReports/resolveReport 已有后端服务，但前端未真正接通。
文件：question-service.ts。